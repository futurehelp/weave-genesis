// index.js (MUST be .js for native to pick it up cleanly)
import { registerRootComponent } from 'expo';
import ExpoRoot from 'expo-router/entry';

// Register the app
registerRootComponent(ExpoRoot);
